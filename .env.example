# Anthropic
ANTHROPIC_API_KEY=             # Required. sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-4-20250514  # Default model

# OpenAI
OPENAI_API_KEY=                # Required if using OpenAI SDK. sk-...
OPENAI_MODEL=gpt-4o            # Default model

# Google Gemini (agent runner uses this when set)
GEMINI_API_KEY=                # From Google AI Studio. Required for agent run if not using Anthropic.
GEMINI_MODEL=gemini-1.5-flash  # Default model

# MCP
MCP_SERVER_URL=                # MCP server endpoint (if remote)
MCP_AUTH_TOKEN=                # MCP auth token

# Hardware (Raspberry Pi)
GPIO_MODE=BCM                  # BCM or BOARD pin numbering
HARDWARE_ENABLED=false         # Set true on Pi hardware

# Runtime
ENVIRONMENT=local              # local | staging | production
LOG_LEVEL=INFO                 # DEBUG | INFO | WARNING | ERROR

# Governance API
DATABASE_URL=sqlite:///agent_ops.db   # SQLite path or postgres URL
API_HOST=0.0.0.0                      # Bind host for API server
API_PORT=8000                          # Bind port for API server
